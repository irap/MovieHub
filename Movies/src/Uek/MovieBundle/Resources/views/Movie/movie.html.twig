{% extends 'base.html.twig' %} 

{% block content %}
<div class="container-fluid">
	<div class="media">
		<div class="media-left">
			<a href="/movie/{{ movie.getId() }}"><img class="media-object"
				src="{{ movie.getCoverArt() }}" alt=""></a>
		</div>
		<div class="media-body">
			<h4 class="media-heading">
				<b>{{ movie.getTitle() }}</b>
			</h4>
			<div class="container-fluid">
			<div class="col-6 col-sm-6 col-lg-4">
			<p> {{ movie.getDescription() }}</p>
			<div>
			</div>
			<p><b>Actors:</b> {{ movie.getActors() }}</p>
			<p><b>Price:</b> ${{ movie.getPrice() }}</p>
			<p><small>reviewed: {{ movie.getReviewNumber() }}  borrowed: {{ movie.getOrderNumber() }}</small></p> 
			
			<p>
				<a href="/movie/{{ movie.getId() }}/watch"
					class="btn btn-default btn-sm" role="button">Watch</a>
				<a href="/movie/{{ movie.getId() }}/review"
					class="btn btn-default btn-sm" role="button">Add Review</a>
				<a href="/movie/{{ movie.getId() }}/borrow"
					class="btn btn-default btn-sm" role="button">Borrow</a>
			</p>
		</div>
	</div>

<div>
{% endblock content %}